{% extends 'base.html.twig' %}

{% block title %}Toutes les recettes{% endblock %}

{% block body %}
    <h1 class="m-3"> Liste des recettes</h1>
    <table class="table">
        <thead>
            <tr>
                <th scope="col">Nom</th>
                <th scope="col">Recette</th>
                <th scope="col">Apperçu</th>
            </tr>
        </thead>
        <tbody>
            {% for recipe in recipes %}
                <tr class="">
                    <td scope="row">
                        <a href="">{{ recipe.name }}</a>
                    </td>
                    <td class="fst-italic"><img src="" alt=""></td>
                    <td><img style="height:125px; width:125px" src="{{ asset(recipe.recipe_previewr) }}" alt="{{ recipe.name }}"></td>
                </tr>
            
            {% endfor %}
        </tbody>
    </table>

{% endblock %}
